import { describe, it, expect } from "vitest";
import { QuadrantScatter } from "./QuadrantScatter";

describe("QuadrantScatter", () => {
  it("is defined", () => {
    expect(QuadrantScatter).toBeDefined();
  });
});
