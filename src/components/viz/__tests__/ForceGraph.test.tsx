import { describe, it, expect } from "vitest";
import { ForceGraph } from "../../viz/ForceGraph";

describe("ForceGraph", () => {
  it("is defined", () => {
    expect(ForceGraph).toBeDefined();
  });
});
