# Degree-ROI pipeline dependencies (extends scripts/requirements.txt)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3      # ROI/premium models, K-Means clusters, TF-IDF + LSA task embeddings
requests>=2.31
openpyxl>=3.1          # read HUD FMR / AIOE / crosswalk .xlsx files

# Phase 3 — O*NET task embeddings. Preferred backend is sentence-transformers
# (all-MiniLM-L6-v2); 03_task_embeddings.py auto-detects it and otherwise falls back to a
# torch-free TF-IDF + TruncatedSVD (LSA) implementation, so the pipeline runs anywhere.
sentence-transformers>=2.2
# torch: install the CPU build to keep it small —
#   pip install "torch>=2.0,<2.6" --index-url https://download.pytorch.org/whl/cpu
