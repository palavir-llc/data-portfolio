# Degree-ROI pipeline dependencies (extends scripts/requirements.txt)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3      # ROI/premium models, K-Means clusters, TF-IDF + LSA task embeddings
requests>=2.31
openpyxl>=3.1          # read HUD FMR / AIOE / crosswalk .xlsx files
# Phase 3 uses sklearn TF-IDF + TruncatedSVD (LSA) for O*NET task embeddings — a
# torch-free, runs-anywhere approach. (A transformer variant via sentence-transformers
# is possible where disk/GPU allow, but is intentionally not required here.)
